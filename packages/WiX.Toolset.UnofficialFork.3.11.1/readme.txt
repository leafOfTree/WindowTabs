WiX.Toolset
===========

IMPORTANT:

When installing or updating the WiX.Toolset package, you MUST restart Visual Studio.
Otherwise the updated WiX Toolset files won't be loaded and you will get build errors, because the old files are still cached by Visual Studio.